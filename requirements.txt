# Core runtime dependencies
python-dotenv>=1.0,<2
pydantic>=2.7,<3
requests>=2.31,<3
beautifulsoup4>=4.12,<5

# LLM + agent stack
openai>=1.35,<2
langchain-openai>=0.2.0,<1

# Headless browser automation (required by browser-use)
playwright>=1.48,<2

# Browser automation orchestrator
# NOTE: main.py (Etherscan flow) comments that it requires browser-use==0.1.45
#       main (2).py (SeiTrace) comments that it uses browser-use==0.2.5
# Pick the version that matches the flow you're running.
browser-use==0.2.5



# Core runtime dependencies
# python-dotenv>=1.0,<2
# pydantic>=2.7,<3
# requests>=2.31,<3
# beautifulsoup4>=4.12,<5

# # LLM + agent stack
# openai>=1.35,<2
# langchain-openai>=0.2.0,<1

# # Browser automation
# playwright>=1.40,<2
# browser-use>=0.6.0,<1